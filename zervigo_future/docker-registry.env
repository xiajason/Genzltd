# Docker镜像源环境变量配置
# 用于统一管理Docker镜像源配置

# 当前使用的镜像源 (默认: 阿里云)
DOCKER_REGISTRY=registry.cn-hangzhou.aliyuncs.com/library

# 备用镜像源
DOCKER_REGISTRY_BACKUP_1=ccr.ccs.tencentyun.com/library
DOCKER_REGISTRY_BACKUP_2=hub-mirror.c.163.com
DOCKER_REGISTRY_BACKUP_3=docker.mirrors.ustc.edu.cn

# 镜像标签
MYSQL_TAG=8.0
REDIS_TAG=7-alpine
POSTGRES_TAG=14-alpine
NEO4J_TAG=latest
CONSUL_TAG=latest
NGINX_TAG=alpine
GOLANG_TAG=1.23-alpine
ALPINE_TAG=latest
PYTHON_TAG=3.11-slim

# 完整的镜像名称
MYSQL_IMAGE=${DOCKER_REGISTRY}/mysql:${MYSQL_TAG}
REDIS_IMAGE=${DOCKER_REGISTRY}/redis:${REDIS_TAG}
POSTGRES_IMAGE=${DOCKER_REGISTRY}/postgres:${POSTGRES_TAG}
NEO4J_IMAGE=${DOCKER_REGISTRY}/neo4j:${NEO4J_TAG}
CONSUL_IMAGE=${DOCKER_REGISTRY}/consul:${CONSUL_TAG}
NGINX_IMAGE=${DOCKER_REGISTRY}/nginx:${NGINX_TAG}
GOLANG_IMAGE=${DOCKER_REGISTRY}/golang:${GOLANG_TAG}
ALPINE_IMAGE=${DOCKER_REGISTRY}/alpine:${ALPINE_TAG}
PYTHON_IMAGE=${DOCKER_REGISTRY}/python:${PYTHON_TAG}

# 应用镜像
BACKEND_IMAGE=jobfirst-backend:latest
USER_SERVICE_IMAGE=jobfirst-user-service:latest
RESUME_SERVICE_IMAGE=jobfirst-resume-service:latest
AI_SERVICE_IMAGE=jobfirst-ai-service:latest

# 使用方法：
# 1. 在docker-compose.yml中引用: ${MYSQL_IMAGE}
# 2. 在构建脚本中source此文件: source docker-registry.env
# 3. 在CI/CD中使用: docker-compose --env-file docker-registry.env up
