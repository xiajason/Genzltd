# Looma CRM AI架构重构 - 环境配置示例 (集成Zervigo子系统)
# 复制此文件为 .env 并修改相应配置

# 应用配置
APP_ENV=development
APP_DEBUG=true
APP_HOST=0.0.0.0
APP_PORT=8888

# ==================== Zervigo子系统集成配置 ====================
# Zervigo统一认证服务
ZERVIGO_AUTH_URL=http://localhost:8623

# Zervigo AI服务
ZERVIGO_AI_URL=http://localhost:8620

# Zervigo简历服务
ZERVIGO_RESUME_URL=http://localhost:8602

# Zervigo职位服务
ZERVIGO_JOB_URL=http://localhost:8609

# Zervigo公司服务
ZERVIGO_COMPANY_URL=http://localhost:8603

# Zervigo用户服务
ZERVIGO_USER_URL=http://localhost:8601

# AI网关服务配置
AI_GATEWAY_PORT=8206
AI_GATEWAY_HOST=0.0.0.0

# AI服务端口配置
RESUME_SERVICE_PORT=8207
MATCHING_SERVICE_PORT=8208
CHAT_SERVICE_PORT=8209
VECTOR_SERVICE_PORT=8210
AUTH_SERVICE_PORT=8211
MONITOR_SERVICE_PORT=8212
CONFIG_SERVICE_PORT=8213

# ==================== 数据库配置 (共享Zervigo现有基础设施) ====================
# Neo4j图数据库 (共享Zervigo现有)
NEO4J_HOST=localhost
NEO4J_PORT=7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=jobfirst_password_2024
NEO4J_DATABASE=neo4j

# Weaviate向量数据库 (共享Zervigo现有)
WEAVIATE_HOST=localhost
WEAVIATE_PORT=8080
WEAVIATE_DATABASE=jobfirst_ai

# PostgreSQL关系数据库 (共享Zervigo现有)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=jobfirst_password_2024
POSTGRES_DB=jobfirst_ai

# Redis缓存 (共享Zervigo现有)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Elasticsearch搜索引擎 (共享Zervigo现有)
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

# MySQL业务数据库 (共享Zervigo现有)
BUSINESS_MYSQL_HOST=localhost
BUSINESS_MYSQL_PORT=3306
BUSINESS_MYSQL_USER=root
BUSINESS_MYSQL_PASSWORD=jobfirst_password_2024
BUSINESS_MYSQL_DB=jobfirst

# 共享基础设施数据库 (共享Zervigo现有)
SHARED_MYSQL_HOST=localhost
SHARED_MYSQL_PORT=3306
SHARED_MYSQL_USER=root
SHARED_MYSQL_PASSWORD=jobfirst_password_2024
SHARED_MYSQL_DB=jobfirst

# AI服务配置
OPENAI_API_KEY=your_openai_api_key_here
DEEPSEEK_API_KEY=your_deepseek_api_key_here
CLAUDE_API_KEY=your_claude_api_key_here

# ==================== JWT配置 (共享Zervigo现有) ====================
JWT_SECRET=jobfirst_jwt_secret_2024
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# 监控配置
PROMETHEUS_HOST=localhost
PROMETHEUS_PORT=9090
GRAFANA_HOST=localhost
GRAFANA_PORT=3000
GRAFANA_ADMIN_PASSWORD=talent_crm_grafana_password

# 日志配置
LOG_LEVEL=INFO
LOG_FILE_PATH=./logs/app.log

# 服务发现配置
SERVICE_REGISTRY_URL=http://localhost:8888/api/cluster/registry
CONSUL_HOST=localhost
CONSUL_PORT=8500

# 跨云部署配置
TENCENT_CLOUD_ENABLED=false
ALIBABA_CLOUD_ENABLED=false
